name: ephemeron-s3

services:
  minio:
    image: cgr.dev/chainguard/minio:latest@sha256:626f73abb00da35765ae77b3dfcd221375b286e956a6bf4e0969a9c16cb5fc1f
    command: ["server", "/data"]
    # Default credentials are fine here: this stack is local/CI-only.
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    healthcheck:
      test: ["CMD", "minio", "--version"]
      interval: 3s
      timeout: 3s
      retries: 10

  minio-init:
    image: cgr.dev/chainguard/minio-client:latest-dev@sha256:aa14fb0ca0a404c239de86b4366929c98f969480dd2be75759a0ed66a946a33b
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: ["/bin/sh", "-c", "mc alias set local http://minio:9000 minioadmin minioadmin && mc mb --ignore-existing local/registry"]

  registry:
    image: registry:3.1.1
    ports:
      - "5000:5000"
    volumes:
      - ./deploy/docker-compose/registry-config-s3.yml:/etc/distribution/config.yml:ro
    depends_on:
      manager:
        condition: service_healthy
      minio-init:
        condition: service_completed_successfully

  manager:
    image: ${MANAGER_IMAGE:-ko.local/ephemeron:latest}
    command: ["serve"]
    environment:
      REDIS_URL: redis://redis:6379
      HOOK_TOKEN: compose-hook-token
      REGISTRY_URL: http://registry:5000
      HOSTNAME_OVERRIDE: localhost:5000
      DEFAULT_TTL: 1h
      MAX_TTL: 24h
      REAP_INTERVAL: 1m
      LOG_FORMAT: json
      PORT: "8000"
      INTERNAL_PORT: "9090"
    ports:
      - "8000:8000"
      - "9090:9090"
    healthcheck:
      test: ["CMD", "/ko-app/cmd", "version"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 3s
    depends_on:
      redis:
        condition: service_healthy

  redis:
    image: redis:8.4-alpine
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 3s
      timeout: 3s
      retries: 10
